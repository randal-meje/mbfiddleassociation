# Copy to .env and fill in the values from https://www.sanity.io/manage after creating
# the Sanity project. .env is gitignored.

# Sanity Studio — reads these (via dotenv) when running `npm run studio`.
SANITY_STUDIO_PROJECT_ID=
SANITY_STUDIO_DATASET=production

# Astro build — reads these (via Vite) at build time when fetching content from Sanity.
# Must match the project above.
PUBLIC_SANITY_PROJECT_ID=
PUBLIC_SANITY_DATASET=production

# Cloudflare Web Analytics (optional) — leave blank to disable. Paste the token from
# https://one.dash.cloudflare.com → Analytics & Logs → Web Analytics → your site → Snippet.
PUBLIC_CF_ANALYTICS_TOKEN=

# Deploy button — Studio only shows the "Deploy" tool when this is set. Point it at the
# Netlify function that proxies the build hook. Leave blank for local Studio dev.
SANITY_STUDIO_DEPLOY_ENDPOINT=https://www.mbfiddleassociation.org/.netlify/functions/deploy

# Server-side only (set in Netlify → Site configuration → Environment variables, NOT
# here in .env — this file is only consulted at local Studio/Astro build time):
#   SANITY_PROJECT_ID        — same value as PUBLIC_SANITY_PROJECT_ID
#   NETLIFY_BUILD_HOOK_URL   — create at Site configuration → Build & deploy →
#                              Build hooks; the function POSTs to it to trigger a build
